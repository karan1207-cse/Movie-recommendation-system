# ─────────────────────────────────────────────────────────
# Streamlit Cloud requirements (app.py — frontend only)
# The backend (main.py) runs on Render, not Streamlit Cloud.
#
# app.py only imports: requests, streamlit
# All ML packages (numpy, pandas, scikit-learn) are used
# only by main.py on the Render backend.
# ─────────────────────────────────────────────────────────
streamlit==1.38.0
requests==2.32.3